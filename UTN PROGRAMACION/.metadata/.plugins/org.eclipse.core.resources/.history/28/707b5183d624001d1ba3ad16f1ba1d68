#include <stdio.h>
#include <stdlib.h>

int main(void) {
	setbuf(stdout, NULL);

	int numeroUno;
	int numeroDos;
	char operador;
	int resultado;

	printf("Ingrese el operador (+/-): ");
	scanf("%c", &operador);
	printf("Ingrese el operador (+/-): ");
	scanf("%d", &numeroUno);
	printf("Ingrese el operador (+/-): ");
	scanf("%d", &numeroDos);

	return 0;
}
